[Pod]
# Name of the pod, gets prefixed with -infra
PodName=crowdsec
# Network, affects all containers attached to pod
Network=pasta:--ipv4-only
# Ports, affects all containers attached to pod
PublishPort=127.0.0.1:8080:8080/tcp
# User namespace
UserNS=auto
